{
  "name": "transaction-engine",
  "version": "0.1.0",
  "description": "transaction engine — a simple transaction engine with idempotency and audit logging",
  "type": "module",
  "main": "dist/server.js",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "migrate": "psql \"$DATABASE_URL\" -f src/database/migrations/001_init.sql",
    "test": "vitest run",
    "test:unit": "vitest run tests/unit",
    "test:integration": "vitest run tests/integration",
    "test:concurrency": "vitest run tests/concurrency"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pg": "^8.12.0",
    "dotenv": "^16.4.5",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "tsx": "^4.16.2",
    "@types/node": "^20.14.11",
    "@types/express": "^4.17.21",
    "@types/pg": "^8.11.6",
    "vitest": "^2.0.4"
  }
}
